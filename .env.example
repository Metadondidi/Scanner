# Claude (Anthropic) API Key
ANTHROPIC_API_KEY=sk-ant-...

# Google Maps URLs for each brand
# Copy the URL directly from Google Maps on the reviews tab
GREEN_SOCIETY_MAPS_URL=https://www.google.com/maps/place/Green+Society/...
RED_SOCIETY_MAPS_URL=https://www.google.com/maps/place/Red+Society/...

# Max reviews to scrape per run (default: 50)
SCRAPE_MAX_REVIEWS=50

# ── Railway-specific (set in Railway dashboard, not locally) ─────────────────
# Path to the SQLite database file — mount a Railway volume at /data
# DATABASE_PATH=/data/reviews.db

# Optional: force a specific Chromium binary (leave empty to use Playwright's)
# CHROMIUM_EXECUTABLE_PATH=/usr/bin/chromium
